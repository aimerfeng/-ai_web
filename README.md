# SkinTech AI Consultant

SkinTech AI Consultant 是一个全栈智能美妆顾问应用，旨在通过 AI 技术为用户提供个性化的护肤建议。系统核心是一个具备"配方师"专业人设的 AI Agent，结合本地 RAG（检索增强生成）知识库和实时网络搜索能力，根据用户的肤质、偏好及历史对话提供精准的护肤方案。

## 🌟 核心功能

- **智能配方师 Agent**：基于专业知识库，提供成分分析、产品推荐和护肤指导。
- **个性化 RAG 知识库**：本地维护 5000+ 美妆产品数据，支持基于成分和功效的精准检索。
- **长短期记忆系统**：
  - **短期记忆**：记录最近 10 轮对话，保持上下文连贯。
  - **长期记忆**：自动提取并更新用户画像（肤质、过敏源、预算等）。
- **时间感知能力**：理解"上周"、"三个月前"等相对时间，基于产品使用周期给出建议。
- **流式对话体验**：采用 Server-Sent Events (SSE) 实现打字机效果，响应流畅。

## 🏗️ 技术架构

本项目采用现代化的前后端分离架构：

- **Frontend**: [Next.js](https://nextjs.org/) + Tailwind CSS
- **Backend**: [FastAPI](https://fastapi.tiangolo.com/) (Python)
- **Vector Database**: [ChromaDB](https://www.trychroma.com/) (本地存储)
- **Relational Database**: SQLite (开启 WAL 模式支持并发)
- **AI Orchestration**: OpenAI API + Tavily Search API

## 📂 目录结构

```
.
├── backend/                # Python FastAPI 后端
│   ├── app/
│   │   ├── api/            # API 路由定义
│   │   ├── core/           # 核心配置与工具
│   │   ├── models/         # Pydantic 与 SQLAlchemy 模型
│   │   ├── services/       # 业务逻辑服务 (Auth, Chat, RAG等)
│   │   └── main.py         # 应用入口
│   └── requirements.txt    # 后端依赖
├── frontend/               # Next.js 前端 (Coming Soon)
├── .kiro/specs/            # 设计与需求文档
├── PUSH_LOG.md             # 推送日志
└── maintain_and_push.ps1   # 自动化维护与推送脚本
```

## 🚀 快速开始

### 环境要求

- Python 3.10+
- Node.js 18+ (前端)
- Git

### 后端设置

1. 进入后端目录（需先创建）：
   ```bash
   cd backend
   ```
2. 创建虚拟环境并激活：
   ```bash
   python -m venv venv
   .\venv\Scripts\Activate.ps1
   ```
3. 安装依赖：
   ```bash
   pip install -r requirements.txt
   ```
4. 启动服务：
   ```bash
   uvicorn app.main:app --reload
   ```

## 📄 文档

- [需求文档](.kiro/specs/skintech-ai-consultant/requirements.md)
- [设计文档](.kiro/specs/skintech-ai-consultant/design.md)
